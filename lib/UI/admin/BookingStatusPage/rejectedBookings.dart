import 'package:flutter/material.dart';

class RejectedBookingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rejected Bookings'),
      ),
      body: Center(
        child: Text('This is the Rejected Bookings Page'),
      ),
    );
  }
}
